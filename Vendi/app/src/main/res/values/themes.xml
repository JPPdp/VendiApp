<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VendiApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>  <!-- Makes status bar transparent -->
        <item name="android:windowTranslucentStatus">true</item> <!-- Extends content to status bar -->
    </style>

</resources>